source "https://rubygems.org"

# Match the GitHub Pages build environment so the site can be built
# both on GitHub and locally with the same dependency set.
gem "github-pages", group: :jekyll_plugins
